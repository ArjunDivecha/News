# CLAUDE.md — News repo

**Before doing any work in this repository, read `openwiki/quickstart.md`.** It
indexes the architecture, analytics, tag-views/allocation, and scenario-risk
docs for the daily report system in `report/`, plus the operations runbook.
Follow its links to whichever subsystem you're touching before editing code.

See also `AGENTS.md` at the repo root for the fuller legacy pipeline history
(universe construction, classification) and learned workspace facts.
